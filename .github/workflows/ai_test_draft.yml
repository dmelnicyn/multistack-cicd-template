# AI Test Draft Generator workflow
# Generates draft pytest test suggestions for changed Python files
# Skips gracefully if OPENAI_API_KEY is not configured or required files are missing

name: AI Test Draft Generator

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

# Prevent concurrent runs on the same PR
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  check-prerequisites:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    outputs:
      should_run: ${{ steps.check.outputs.should_run }}
    steps:
      - uses: actions/checkout@v6
      
      - name: Check prerequisites
        id: check
        env:
          HAS_SECRET: ${{ secrets.OPENAI_API_KEY != '' }}
        run: |
          # Check if secret is configured
          if [ "$HAS_SECRET" != "true" ]; then
            echo "::notice::OPENAI_API_KEY not configured, skipping AI test draft"
            echo "should_run=false" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          # Check if required files exist
          if [ ! -f "Makefile" ]; then
            echo "::notice::Makefile not found (template mode), skipping AI test draft"
            echo "should_run=false" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          if [ ! -f "tools/ai_test_draft.py" ]; then
            echo "::notice::tools/ai_test_draft.py not found, skipping AI test draft"
            echo "should_run=false" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          echo "should_run=true" >> $GITHUB_OUTPUT

  generate-tests:
    needs: check-prerequisites
    if: needs.check-prerequisites.outputs.should_run == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Mask secrets
        run: echo "::add-mask::${{ secrets.OPENAI_API_KEY }}"

      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Install dependencies
        run: make install

      - name: Generate draft tests
        run: uv run python tools/ai_test_draft.py
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          REPO: ${{ github.repository }}

      - name: Upload draft tests artifact
        uses: actions/upload-artifact@v6
        with:
          name: draft-tests
          path: artifacts/draft_tests.md
          retention-days: 7
          if-no-files-found: ignore
